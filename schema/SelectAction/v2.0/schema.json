{
  "$id": "https://schema.nfh.global/SelectAction/v2.0",
  "x-iri": "https://schema.nfh.global/SelectAction/v2.0",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Select Action",
  "description": "Beckn /beckn/select message payload. Sent by a BAP to a BPP to select items and offers from a catalog, initiating the negotiation cycle. (Context wrapper stripped; only the message-content portion is inlined.)",
  "additionalProperties": false,
  "properties": {
    "contract": {
      "$ref": "https://schema.nfh.global/Contract/v2.0/schema.json"
    }
  },
  "required": [
    "contract"
  ]
}
