{
  "$id": "https://schema.nfh.global/StatusAction/v2.0",
  "x-iri": "https://schema.nfh.global/StatusAction/v2.0",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Status Action",
  "description": "Beckn /beckn/status message payload. Sent by a BAP to a BPP to query the current state of a contract/order by its identifier. (Context wrapper stripped; only the message-content portion is inlined.)",
  "properties": {
    "contract": {
      "allOf": [
        {
          "$ref": "https://schema.nfh.global/Contract/v2.0/schema.json"
        },
        {
          "required": [
            "id"
          ]
        }
      ]
    }
  },
  "required": [
    "contract"
  ]
}
