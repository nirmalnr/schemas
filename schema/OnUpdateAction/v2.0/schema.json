{
  "$id": "https://schema.nfh.global/OnUpdateAction/v2.0",
  "x-iri": "https://schema.nfh.global/OnUpdateAction/v2.0",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "On Update Action",
  "description": "Beckn /beckn/on_update message payload. Sent by a BPP to a BAP in response to a /beckn/update call (or as an unsolicited update push), returning the updated contract state. (Context wrapper stripped; only the message-content portion is inlined.)",
  "additionalProperties": false,
  "properties": {
    "contract": {
      "$ref": "https://schema.nfh.global/Contract/v2.0/schema.json"
    }
  },
  "required": [
    "contract"
  ]
}
