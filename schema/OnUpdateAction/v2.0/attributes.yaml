openapi: 3.1.1
info:
  title: OnUpdateAction
  version: 2.0.0
  description: "Container schemas fetched from beckn.yaml. This cannot be extended as it is a reserved schema in beckn protocol. Any additional properties added to this schema can only be made using its *Attributes property"
  license:
    name: CC-BY-NC-SA 4.0 International
  contact:
    name: Beckn Labs
    url: https://beckn.io
components:
  schemas:
    OnUpdateAction:
      x-iri: https://schema.nfh.global/OnUpdateAction/v2.0
      title: On Update Action
      description: 'Beckn /beckn/on_update message payload. Sent by a BPP to a BAP
        in

        response to a /beckn/update call (or as an unsolicited update push),

        returning the updated contract state.

        (Context wrapper stripped; only the message-content portion is inlined.)'
      additionalProperties: false
      properties:
        contract:
          $ref: https://schema.nfh.global/Contract/v2.0/attributes.yaml#/components/schemas/Contract
      required:
      - contract
