{
  "$id": "https://schema.nfh.global/InitAction/v2.0",
  "x-iri": "https://schema.nfh.global/InitAction/v2.0",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Init Action",
  "description": "Beckn /beckn/init message payload. Sent by a BAP to a BPP to initialise a contract with consumer details (billing address, fulfillment preferences, etc.). (Context wrapper stripped; only the message-content portion is inlined.)",
  "additionalProperties": false,
  "properties": {
    "contract": {
      "$ref": "https://schema.nfh.global/Contract/v2.0/schema.json"
    }
  },
  "required": [
    "contract"
  ]
}
