openapi: 3.1.1
info:
  title: ConfirmAction
  version: 2.0.0
  description: "Container schemas fetched from beckn.yaml. This cannot be extended as it is a reserved schema in beckn protocol. Any additional properties added to this schema can only be made using its *Attributes property"
  license:
    name: CC-BY-NC-SA 4.0 International
  contact:
    name: Beckn Labs
    url: https://beckn.io
components:
  schemas:
    ConfirmAction:
      x-iri: https://schema.nfh.global/ConfirmAction/v2.0
      title: Confirm Action
      description: 'Beckn /beckn/confirm message payload. Sent by a BAP to a BPP to
        confirm

        a contract, finalising the transaction terms agreed during the

        select-init negotiation cycle.

        (Context wrapper stripped; only the message-content portion is inlined.)'
      properties:
        contract:
          $ref: https://schema.nfh.global/Contract/v2.0/attributes.yaml#/components/schemas/Contract
      required:
      - contract
