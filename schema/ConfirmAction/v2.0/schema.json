{
  "$id": "https://schema.nfh.global/ConfirmAction/v2.0",
  "x-iri": "https://schema.nfh.global/ConfirmAction/v2.0",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Confirm Action",
  "description": "Beckn /beckn/confirm message payload. Sent by a BAP to a BPP to confirm a contract, finalising the transaction terms agreed during the select-init negotiation cycle. (Context wrapper stripped; only the message-content portion is inlined.)",
  "properties": {
    "contract": {
      "$ref": "https://schema.nfh.global/Contract/v2.0/schema.json"
    }
  },
  "required": [
    "contract"
  ]
}
