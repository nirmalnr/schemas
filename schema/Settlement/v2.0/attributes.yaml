openapi: 3.1.1
info:
  title: Settlement
  version: 2.0.0
  description: "Container schemas fetched from beckn.yaml. This cannot be extended as it is a reserved schema in beckn protocol. Any additional properties added to this schema can only be made using its *Attributes property"
  license:
    name: CC-BY-NC-SA 4.0 International
  contact:
    name: Beckn Labs
    url: https://beckn.io
components:
  schemas:
    Settlement:
      x-iri: https://schema.nfh.global/Settlement/v2.0
      type: object
      properties:
        id:
          type: string
        considerationId:
          $ref: https://schema.nfh.global/Consideration/v2.0/attributes.yaml#/components/schemas/Consideration/properties/id
        status:
          type: string
          enum:
          - DRAFT
          - COMMITTED
          - COMPLETE
        settlementAttributes:
          $ref: https://schema.nfh.global/Attributes/v2.0/attributes.yaml#/components/schemas/Attributes
