openapi: 3.1.1
info:
  title: Participant
  version: 2.0.0
  description: "Container schemas fetched from beckn.yaml. This cannot be extended as it is a reserved schema in beckn protocol. Any additional properties added to this schema can only be made using its *Attributes property"
  license:
    name: CC-BY-NC-SA 4.0 International
  contact:
    name: Beckn Labs
    url: https://beckn.io
components:
  schemas:
    Participant:
      x-iri: https://schema.nfh.global/Participant/v2.0
      type: object
      properties:
        id:
          type: string
        descriptor:
          $ref: https://schema.nfh.global/Descriptor/v2.1/attributes.yaml#/components/schemas/Descriptor
        participantAttributes:
          description: 'Domain-specific extension attributes for this contract.

            Use beckn:LogisticsContract for hyperlocal physical delivery. Use the
            generic Attributes schema for

            other fulfillment types where no well-known domain schema exists.'
          allOf:
          - $ref: https://schema.nfh.global/Attributes/v2.0/attributes.yaml#/components/schemas/Attributes
          - properties: {}
