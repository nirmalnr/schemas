openapi: 3.1.1
info:
  title: SupportAction
  version: 2.0.0
  description: "Container schemas fetched from beckn.yaml. This cannot be extended as it is a reserved schema in beckn protocol. Any additional properties added to this schema can only be made using its *Attributes property"
  license:
    name: CC-BY-NC-SA 4.0 International
  contact:
    name: Beckn Labs
    url: https://beckn.io
components:
  schemas:
    SupportAction:
      x-iri: https://schema.nfh.global/SupportAction/v2.0
      title: Support Action
      description: 'Beckn /beckn/support message payload. Sent by a BAP to a BPP to
        request

        support contact information or to open a support ticket for an existing

        order/contract.

        (Context wrapper stripped; only the message-content portion is inlined.)'
      type: object
      additionalProperties: false
      properties:
        support:
          $ref: https://schema.nfh.global/Support/v2.0/attributes.yaml#/components/schemas/Support
      required:
      - support
