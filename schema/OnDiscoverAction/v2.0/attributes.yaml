openapi: 3.1.1
info:
  title: OnDiscoverAction
  version: 2.0.0
  description: "Container schemas fetched from beckn.yaml. This cannot be extended as it is a reserved schema in beckn protocol. Any additional properties added to this schema can only be made using its *Attributes property"
  license:
    name: CC-BY-NC-SA 4.0 International
  contact:
    name: Beckn Labs
    url: https://beckn.io
components:
  schemas:
    OnDiscoverAction:
      x-iri: https://schema.nfh.global/OnDiscoverAction/v2.0
      description: The on_discover response payload containing matching catalogs.
      type: object
      required:
      - catalogs
      properties:
        catalogs:
          type: array
          description: 'Array of catalogs matching the discovery request.

            Array of catalogs matching the discovery request.'
          items:
            $ref: https://schema.nfh.global/Catalog/v2.1/attributes.yaml#/components/schemas/Catalog
      additionalProperties: false
